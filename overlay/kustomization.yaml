apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default

commonLabels:
  app: wordpress-challenge

resources:
- ../base
- pvc.yaml

patches:
  - path: storage_patch.yaml
  - path: db_storage_patch.yaml

patchesStrategicMerge:
  - login_patch.yaml
